# NOVA 超星計畫 CMS 管理後台

網址：`https://<你的網域>/admin/`（本機開發：`http://localhost:4179/admin/`）

## 架構總覽

```
編輯流程：/admin 登入 → 改欄位 → 儲存草稿 → ?preview=1 預覽 → 發布更新（觸發重建）→ 1–2 分鐘後官網生效

資料層：Supabase（與 XP 共用專案 ixvvoqywxddpjuzqmetb，nova_ 前綴完全隔離）
├── nova_pages      頁面（目前只有「首頁」，未來加頁面＝加一筆）
├── nova_sections   區塊（草稿 content / 已發布 published_content 雙欄 + enabled 開關）
├── nova_admins     管理員 email 白名單（RLS 依此判斷寫入權；三級角色）
├── nova_settings   設定（deploy_hook 等；owner 才能寫）
├── nova_leads      預約諮詢名單（官網表單即時進來）
└── Storage nova    上傳的圖片素材（公開讀取，寫入僅白名單）

前端：
├── index.html            官網本體＝內容單一來源（data-cms 標注，內建文案＝預設值）
├── assets/cms.js         共用資料層（抓取／套用內容／送出表單；build 與瀏覽器共用）
├── scripts/build.mjs     建置：把「已發布」內容烘進靜態 HTML → dist/
└── admin/                後台 SPA（表單自動從官網標注生成，官網加欄位後台自動跟上）
```

## 權限模型（三級角色，資料庫層強制）

| 角色 | 編輯草稿 | 預覽 | 顯示/隱藏區塊 | 發布 | 系統設定 | 帳號管理 |
|------|------|------|------|------|------|------|
| `owner` 最高管理者 | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| `admin` 管理員 | ✓ | ✓ | ✓ | ✓ | ✕ | ✕ |
| `editor` 操作人員 | ✓ | ✓ | ✕ | ✕ | ✕ | ✕ |

- 權限在**資料庫層（RLS ＋ 觸發器）強制**，繞過介面直接打 API 一樣被擋——已實測 7 項：
  editor 改開關 ✕、editor 發布 ✕、editor 讀設定 ✕、editor 自我升權 ✕、匿名讀名單 ✕、editor 存草稿 ✓、匿名送表單 ✓。
- 白名單與角色在 `nova_admins`；登入用 Supabase Auth（與 XP CMS 同一組帳密，權限各自獨立）。
- 移除白名單**立即生效**；anon key 是公開金鑰（設計如此），安全由 RLS 把關。

## 草稿 → 預覽 → 發布

- **儲存不會動到正式站**：草稿與已發布是兩份資料，正式站 build 只讀已發布快照。
- 預覽：官網任何頁面加上 `?preview=1` 即時渲染草稿（畫面下方有「草稿預覽模式」標記）。
- 「發布更新」＝全站草稿快照為正式版 ＋ 觸發 Deploy Hook 重建（admin 以上才能按）。

## 首次啟用（部署到 Cloudflare Pages 後一個步驟）

設定 Deploy Hook：Cloudflare Pages 專案 → Settings → Builds & deployments → Deploy hooks
→ Create → 複製 URL → 後台「網站設定 → 部署」貼上儲存。詳見 `DEPLOY_CLOUDFLARE.md`。

## 日常操作

- **改文案／圖片**：`/admin` → 點區塊展開 → 改欄位 → 「儲存此區塊」→ 預覽 → 發布。
- **表單欄位＝官網現值**；「重設為預設值」清除所有覆寫、回到程式內建文案。
- **換圖**：圖片欄位可「上傳檔案」（進 Supabase Storage）或直接貼 URL。
- **作品版位**：04 各專案班的「作品 N · 圖片」上傳後即取代 IMAGE SLOT 佔位。
- **隱藏區塊**：區塊卡右側開關（管理員以上）；隱藏 tab-XXX 會連 Tab 按鈕一起隱藏。
- **調整區塊順序**：區塊卡 ↑↓（管理員以上）；於下次發布重建後生效。
- **看名單**：側欄「預約諮詢名單」——官網表單即時進來（可匯出 CSV；管理員以上可刪），**不需重新發布**。
- **加成員**：權限管理 → 填 email 選角色 → 「+ 加入」→ 自動建立登入帳號並顯示**臨時密碼（僅一次）**。
- **忘記密碼**：權限管理 → 「重設密碼」產生新密碼（舊密碼立即失效）。密碼經雜湊儲存、無法回查。

## 注意事項

- **文字欄位清空＝使用程式內建預設值**；**清單刪光＝該群組整個不顯示**（明確意圖）。
- 後台表單由官網 `data-cms` 標注自動生成——官網改版加欄位，後台**自動跟上**，不用改後台程式。
- Hero 3D 徽章、數字 shuffle、效益長條寬度為程式化視覺，v1 不開放編輯（同 XP「生態圈」原則）。
- 後台改完**必須發布**（重建）官網才會變——內容烘進靜態 HTML，對 SEO 與速度最有利。
- `/admin` 頁面公開可達，但沒登入＋不在白名單就只有登入畫面、拿不到任何資料。

## 擴充新頁面（未來）

1. `nova_pages` 加一筆（slug 如 `about`）、`nova_sections` 建立該頁區塊。
2. 新頁 HTML 用同一套 `data-cms="區塊.欄位"` 標注 + `scripts/build.mjs` 比照處理。
3. 後台自動列出（表單從標注生成）。
